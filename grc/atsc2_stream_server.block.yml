# auto-generated by grc.converter

id: atsc2_stream_server
label: TS Streaming Server
category: '[atsc2]'

parameters:
-   id: port
    label: Port
    dtype: int
    default: '0'

inputs:
-   domain: stream
    dtype: byte
asserts:
- ${ port > 0 }

templates:
    imports: import atsc2
    make: atsc2.tcp_sink(${port})

file_format: 1
